import { FormatValPipe } from './format-val.pipe';

describe('FormatValPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatValPipe();
    expect(pipe).toBeTruthy();
  });
});
